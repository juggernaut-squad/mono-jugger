import { styled } from '@mui/material/styles';

export const Root = styled('div')(({ theme }) => ({
	background: '#212121',
	color: '#fefefe',
	display: 'grid',
	placeContent: 'center',
	width: '100%',
	height: '56px',
}));
